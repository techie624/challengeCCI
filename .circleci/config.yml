version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     # start proprietary DB using private Docker image
     # with credentials stored in the UI
     # build the application image
     - run:
        command: |
          echo Running test
          set +e
          mkdir -p /tmp/test-results
          make test
          pwd=`pwd` && echo $pwd
     
     - run: docker build . -t cci-nginx
